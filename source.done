import "source2.done"
import "source2.done"

fun sum(x : int, y : int) : int {
   return x + y;
}

fun main() : int {
   var name : string = "Amr Hesham"
   var x : int = 10;
   var y : int = 20;
   var sum : int = sum(x, y);
   return 0;
}